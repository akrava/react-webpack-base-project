# react-webpack-base-project

## Base web project using React framework

### #react, #webpack, #babel, #express

To start project, do `npm i` and then `npm run dev`

---------
Ready to deploy on Heroku