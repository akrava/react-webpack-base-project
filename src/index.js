import '@babel/polyfill';
import React from "react";
import ReactDOM from "react-dom";

const root = (
  <main>
    <h1>Hello world!</h1>
    <p>Edit <code>src/index.js</code> to start</p>
  </main>
);

ReactDOM.render(root, document.getElementById('root'));